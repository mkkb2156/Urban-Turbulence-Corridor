# Vercel Python runtime dependencies (API only)
fastapi>=0.104
uvicorn>=0.24
python-dotenv>=1.0
requests>=2.31
pydantic>=2.5

# Database (optional — API gracefully handles missing DB)
sqlalchemy>=2.0
psycopg2-binary>=2.9

# Geo (needed for corridor endpoint)
geopandas>=0.14
shapely>=2.0
pyproj>=3.6

# Data processing (used by wind rose / stats)
numpy>=1.25
pandas>=2.1
